<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fake Trello</title>
  <link rel="shortcut icon" href="https://cdn.iconscout.com/icon/free/png-256/trello-9-722650.png"/>
  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css">
</head>
<body>
  <nav class="flex justify-between items-center bg-blue-400 text-blue-100 text-xl px-4 py-2">
    <a href="/">
      <i class="fab fa-trello"></i> Fake Trello
    </a>
    
    <div class="my-1">
      {{#if user}}
      <span class="mr-2">{{user.email}}</span>
      <a class="px-3 py-2 bg-blue-300 rounded-sm" href="/logout">登出</a>
      {{else}}
      <a class="px-3 py-2 bg-blue-300 rounded-sm" href="/signIn">登入</a>
      <a class="px-3 py-2 bg-blue-300 rounded-sm" href="/signUp">註冊</a>
      {{/if}}
    </div>
  </nav>
  <main role="main">
    <div class="album py-2 bg-light">
      <div class="container">
        {{#if success_messages}}
        <div class="alert alert-success" role="alert" style="margin: 30px 0 ;">
          {{success_messages}}
        </div>
        {{/if}}
        {{#if error_messages}}
        <div class="alert alert-danger" role="alert" style="margin: 30px 0 ;">
          {{error_messages}}
        </div>
        {{/if}}        
      </div>
    </div>
  </main>
  {{{body}}}
  <script src="https://kit.fontawesome.com/3285f63162.js" crossorigin="anonymous"></script>
</body>
</html>